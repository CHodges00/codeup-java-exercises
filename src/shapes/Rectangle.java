package shapes;

public class Rectangle extends Shape implements Measurable{


    @Override
    public double perimeter() {
        return perimeter();
    }

    @Override
    public double area() {
        return area();
    }
}
